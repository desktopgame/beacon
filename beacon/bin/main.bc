namespace run55 {
	class Base {
		public:
		property Int A {
			defset;
			defget;
		}

		property Int age {
			defset;
			defget;
		}

		property static String name {
			defset;
			defget;
		}

		def new() {
		}
	}
}
var d = new run55::Base();
//d.age = 0;
d.A = 10;
printLine(run55::Base.name);
printLine(d.age);
printLine(d.A);
run55::Base.name = "Hello";
printLine(run55::Base.name);