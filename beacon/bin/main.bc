namespace run68 {
	class AAA {
		public:
		Vector[Int] v = new Vector[Int]() << 1 << 3 << 5;

		def new() {
		}
	}
}
var a = new run68::AAA();
var iter = a.v.iterate();
while(iter.moveNext()) {
	iter.current().printLine();
}