namespace thread {
	class MyRunnable : Runnable {
		public:
		def new() {
		}

		def run() -> Void {
			var i = 0;
			while(i < 10) {
				printLine("print " + i);
				i = i + 1;
			}
		}
	}
}
var th = new Thread("thread", new thread::MyRunnable());
th.start();
th.join();