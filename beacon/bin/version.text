beacon
	VERSION:	$version
	TIME:		$time

history
	ALPHA0
		最初のリリース

	ALPHA1
		- 内部的に同じファイルに二回クラスローダーが生成されていたのを修正
		- 定義されていなければ !=(Object) を自動で実装するように
		- トップレベルでも this を参照可能に
		- 明示的な演算子の呼び出しを実装
		- 整数型をプールから参照するように
		- メモリリークの修正