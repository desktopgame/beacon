var v = new Vector[String]();
v.push("SSS");
v.push("AXIS");
v.push("XYZ");
var iter = v.iterate();
while(iter.hasNext()) {
	Console.writeLine(iter.next());
}
Console.writeLine("---");
v.remove(1);
var iter2 = v.iterate();
while(iter2.hasNext()) {
	Console.writeLine(iter2.next());
}