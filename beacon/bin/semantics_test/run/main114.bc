namespace run114 {
	interface Walkable {
		public:
		def walk() -> Void;
	}
	interface Flyable {
		public:
		def fly() -> Void;
	}
	class Bird : Walkable, Flyable {
		public:
		def new() {
		}

		def walk() -> Void {
			printLine("walk");
		}

		def fly() -> Void {
			printLine("fly");
		}
	}
}
var bird = new run114::Bird();
run114::Walkable w = bird;
run114::Flyable f = bird;
w.walk();
f.fly();