namespace err30 {
	class AccessTest {
		public:
		def new() {
		}

		def publicPrintLine() -> Void {
			printLine("public");
		}

		private:
		def privatePrintLine() -> Void {
			printLine("private");
		}
	}
}
var a = new err30::AccessTest();
a.publicPrintLine();
a.privatePrintLine();