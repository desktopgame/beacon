import "Object"
import "Array"
import "Int"
import "Double"
import "Char"
import "String"
import "Bool"
import "Null"
import "Void"
import "Console"

import "Exception"
import "StackTraceElement"

var arr = new Array<|Int|>(3);
arr.set(0, 10);
arr.set(1, 20);
arr.set(2, 30);

var iter = arr.iterate();
while(iter.hasNext()) {
	var ival = iter.next();
	Console.writeLine(ival);
}
