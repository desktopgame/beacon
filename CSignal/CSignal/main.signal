import "lib"
//import "../io.h"
namespace L.Goo.Foo {
	class Foo : SuperFoo {
		int pointX;
		int pointY;
	
		def print(String message, Int d) -> Void {
			main();
			main(10, 20, "aaa");
			a.print();
			a.print(10, 20, "aaa");
			if(10) {

			}

			if('a' || 10) {

			} elif("hello") {

			}

			if(40 == 40) {

			} elif(50 == 50) {

			} else {

			}
		}
	}
	namespace M {
		class Klass {
		}
	}
}

//(10 + 20 + (5 * 20 + (16 / 4)) >= 20 || 0 < 0)
//TYPENAME VARIABLENAME = 10;
//STRING STR = "aaaa";
//CHAR CH = 'z';