import "lib"
import "Vector"

var arr = new Array(5);
var offs = 0
while(offs < arr.length()) {
	arr.set(offs, offs * 10)
	offs++
}

var itr = arr.iterate()
while(itr.hasNext()) {
	var e = itr.next()
	Console.writeLine(e)
}